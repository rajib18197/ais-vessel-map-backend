feat: add config, DB layer, AIS feed pipeline, and server bootstrap

Config
- Add Zod-validated env schema with fail-fast startup (env.ts)
- Extract domain and infra constants to constants.ts:
  HEADING_NOT_AVAILABLE, POSITION_REPORT_TYPES, STATIC_REPORT_TYPES,
  ACTIVE_VESSEL_WINDOW_MS, STALE_VESSEL_THRESHOLD_MS, CLEANUP_INTERVAL_MS,
  RATE_LIMIT_WINDOW_MS, RATE_LIMIT_MAX_REQUESTS

Database
- Add MongoDB connection with bufferCommands:false and 10s
  serverSelectionTimeout for fail-fast startup (connection.ts)
- Guard registerConnectionEventLogging() against duplicate listener
  registration across reconnects
- Derive isDBConnected() from mongoose.connection.readyState - single
  source of truth, no local flag drift
- Add GeoPoint tuple type to vessel.model.ts; Mongoose cannot express
  fixed-length coordinate tuples via InferSchemaType so this is defined
  separately and used as the validated coordinate contract
- Add 2dsphere sparse index on location and btree index on lastSeen

AIS feed
- Add discriminated union VesselUpdate (VesselPositionUpdate |
  VesselStaticUpdate) with hasPosition flag for compile-time narrowing
- Add Zod runtime validation on raw decoder output at the external
  boundary (parse don't validate)
- Resolve ais-stream-decoder CJS/ESM interop at module load time;
  throw immediately if constructor cannot be found
- Add TCP/UDP connection service with line-buffering, auto-reconnect,
  and clean shutdown (no EADDRINUSE on UDP restart)
- Add applyVesselUpdate usecase: upserts vessel by MMSI, emits
  vessel:created or vessel:updated on the typed event bus

App / Server
- Add createApp() factory with helmet, CORS, hpp, compression,
  rate limiting, and request logger; kept side-effect-free for testability
- Add server bootstrap with graceful SIGTERM/SIGINT shutdown,
  uncaughtException and unhandledRejection guards, and forced
  exit after 10s timeout