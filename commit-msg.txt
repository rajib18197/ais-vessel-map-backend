feat: add vessel query slices, WebSocket stream, and cleanup job

get-all-vessels
- Add GET /api/vessels returning vessels active within the last 15 min
- Parse DB output through z.array(vesselSummarySchema) at the boundary;
  coordinates enforced as z.tuple([z.number(), z.number()]) not z.array
- Chain .exec() on Mongoose query for proper Promise and stack trace support
- Add usecase tests covering empty result, active window cutoff using
  ACTIVE_VESSEL_WINDOW_MS constant, and response shape

stream-vessels
- Add WebSocket handler at /ws/vessels with ping/pong heartbeat
  (30s interval) and dead socket termination
- Send vessel:snapshot on connection so the map is never empty on
  first connect — new clients receive current active vessels immediately
- Subscribe to vessel:updated and vessel:created on the typed event bus;
  broadcast serialized payload to all connected clients
- Clean up heartbeat timer and event listeners on wss close to prevent
  memory leaks
- Add toGeoPoint() replacing toCoordinatePair(): validates coordinate
  tuple length and guards undefined elements under noUncheckedIndexedAccess;
  returns typed GeoPoint or null instead of raw number[]

cleanup-vessels
- Add background job that deletes vessels unseen for 24 hours
- Runs once immediately on startCleanupJob() then every 30 minutes
- Uses STALE_VESSEL_THRESHOLD_MS and CLEANUP_INTERVAL_MS from constants
- stopCleanupJob() clears the interval cleanly for graceful shutdown

App wiring
- Add vesselsRouter composing vessel slices under /api/vessels; in-bounds
  registered before /:mmsi to prevent param route shadowing
- Wire requestLogger into createApp() global middleware chain
- Add registerVesselStream and startCleanupJob to server bootstrap
- Add WebSocketServer on shared HTTP port at /ws/vessels path